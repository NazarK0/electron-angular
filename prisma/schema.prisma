generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
}

datasource db {
  provider = "sqlite"
  url      = "file:../test.db"
}

// models
model User {
  id          Int       @id @default(autoincrement())
  login       String
  password    String
}

